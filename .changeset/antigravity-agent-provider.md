---
"@ai-hero/sandcastle": minor
---

Add Google Antigravity CLI (`antigravity` / `agy`) agent provider with support for streaming JSON output, stdin prompt delivery, interactive mode, reasoning effort, execution modes, subagents, slash command controls, scaffold `~/.gemini` credential mounts injection, and Next Steps dual authentication guidance.
