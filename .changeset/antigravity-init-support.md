---
"@ai-hero/sandcastle": minor
---

Add `sandcastle init` support for Google Antigravity (`antigravity` / `agy`), including Dockerfile and `.env.example` generation, `~/.gemini` credential mounting for Google AI Pro authentication, and Next Steps guidance.
